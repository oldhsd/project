'use client';
import { useState } from 'react'; import { Check, Plus, Save } from 'lucide-react';
const initial = { title: '', company: '', type: 'Internship', location: 'Remote', mode: 'Remote', description: '', skills: '', eligibility: '', deadline: '' };
export function AdminStudio(){const [form,setForm]=useState(initial),[status,setStatus]=useState('');function change(e:React.ChangeEvent<HTMLInputElement|HTMLTextAreaElement|HTMLSelectElement>){setForm({...form,[e.target.name]:e.target.value})}async function submit(e:React.FormEvent){e.preventDefault();setStatus('Saving…');const r=await fetch('/api/opportunities',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...form,skills:form.skills.split(',').map(x=>x.trim()).filter(Boolean),status:'published',featured:false})});setStatus(r.ok?'Published. It is now visible in Opportunities.':'Could not publish. Check each required field.');if(r.ok)setForm(initial)}return <div className="grid gap-6 xl:grid-cols-[minmax(0,1.2fr)_340px]"><form onSubmit={submit} className="panel p-6"><div className="mb-6 flex items-center gap-2"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[var(--coral)] text-[#37140e]"><Plus className="h-4 w-4" /></span><div><h2 className="font-black">Publish opportunity</h2><p className="muted text-xs">A role is visible only after you publish it.</p></div></div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-bold">Role title<input required name="title" value={form.title} onChange={change} className="input mt-1" placeholder="Frontend Intern" /></label><label className="text-sm font-bold">Company<input required name="company" value={form.company} onChange={change} className="input mt-1" placeholder="BuildNext Partner" /></label><label className="text-sm font-bold">Type<select name="type" value={form.type} onChange={change} className="input mt-1"><option>Internship</option><option>Fellowship</option><option>Competition</option><option>Job</option></select></label><label className="text-sm font-bold">Work mode<input required name="mode" value={form.mode} onChange={change} className="input mt-1" placeholder="Remote" /></label><label className="text-sm font-bold">Location<input required name="location" value={form.location} onChange={change} className="input mt-1" placeholder="Remote / Mumbai" /></label><label className="text-sm font-bold">Deadline<input name="deadline" value={form.deadline} onChange={change} type="date" className="input mt-1" /></label></div><label className="mt-4 block text-sm font-bold">About this role<textarea required name="description" value={form.description} onChange={change} className="input mt-1 min-h-28" placeholder="What will the student work on?" /></label><label className="mt-4 block text-sm font-bold">Skills<input name="skills" value={form.skills} onChange={change} className="input mt-1" placeholder="React, Figma, communication" /></label><label className="mt-4 block text-sm font-bold">Eligibility<input name="eligibility" value={form.eligibility} onChange={change} className="input mt-1" placeholder="Open to 2nd–4th year students" /></label><div className="mt-6 flex items-center gap-3"><button className="btn btn-primary"><Save className="h-4 w-4" />Publish role</button>{status&&<p className="text-sm muted">{status}</p>}</div></form><aside className="space-y-4"><div className="panel p-5"><p className="eyebrow">OPERATIONS</p><h2 className="mt-1 text-xl font-black">Content queue</h2><div className="mt-5 space-y-4">{[['Tracks','3 active'],['Events','2 upcoming'],['Opportunities','4 published'],['Reviews','7 pending']].map(([label,value])=><div className="flex justify-between text-sm" key={label}><span className="muted">{label}</span><span className="font-bold">{value}</span></div>)}</div></div><div className="rounded-xl bg-[var(--coral)] p-5 text-[#37140e]"><Check className="h-5 w-5" /><p className="mt-3 font-black">Consent is built in.</p><p className="mt-1 text-sm">A student application shares profile data only after explicit consent.</p></div></aside></div>}
